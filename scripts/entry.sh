#/bin/sh

#One Script to rule them all, One Script to find them,
#One Script to bring them all, and in the arweave bind them.

source gitClone.sh
cd /home/node/ardrive-cli
source /home/node/yarnInstall.sh
